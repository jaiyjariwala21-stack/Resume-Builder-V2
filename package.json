{
  "name": "job-machine-resume-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Stateless AI resume builder and job matcher for Netlify.",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy",
    "deploy:prod": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^22.2.0"
  }
}
